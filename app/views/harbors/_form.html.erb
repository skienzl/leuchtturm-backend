<%= form_for(@harbor) do |f| %>
  <% if @harbor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@harbor.errors.count, "error") %> prohibited this harbor from being saved:</h2>

      <ul>
      <% @harbor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
