<ul class="nav nav-sidebar">
  <% @harbor.zones.each do |zone| %>
    <%= active_li_link_to zone.name, edit_harbor_zone_path(@harbor, zone) %>
  <% end %>
  <%= active_li_link_to t('create'), new_harbor_zone_path(@harbor) unless @harbor.id.nil?%>
  <%= active_li_link_to t('back'), harbors_path %>
</ul>